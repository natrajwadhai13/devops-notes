---
title: "2. Installation and Setup"
parent: "• Helm"
nav_order: 2
---

# Installation and Setup

## Install and Verify

```bash
helm version
kubectl version --client
kubectl get nodes
```

Helm needs access to a Kubernetes cluster for commands that install or manage releases.

## Repository Setup

```bash
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update
helm repo list
helm search repo nginx
```

## Test Installation

```bash
helm install my-nginx bitnami/nginx
helm list
helm status my-nginx
helm uninstall my-nginx
```

## Basic Setup Flow

```text
Install Helm
   ↓
Verify kubectl / cluster
   ↓
Add chart repository
   ↓
Update repository index
   ↓
Search chart
   ↓
Install test release
   ↓
Verify and uninstall
```
