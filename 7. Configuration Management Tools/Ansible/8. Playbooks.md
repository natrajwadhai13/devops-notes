---
title: "8. Playbooks"
nav_order: 8
parent: "• Ansible"
grand_parent: "7. Configuration Management Tools"
has_children: true
---

# 8. Playbooks

## Learning Objectives

After completing this chapter, you will be able to:

- Understand what a Playbook is.
- Learn the structure of a Playbook.
- Execute Playbooks.
- Understand Plays and Tasks.
- Use Become (Privilege Escalation).
- Understand Gather Facts.
- Access Ansible Facts.
- Create Multiple Plays.
- Understand Playbook execution order.
- Handle errors in Playbooks.
- Follow Playbook best practices.

---

# What is a Playbook?

A **Playbook** is a YAML file that defines one or more automation tasks.

Instead of running multiple Ad-hoc Commands, you can store all tasks in a single Playbook and execute them whenever needed.

Example:

```text
Install Nginx
↓

Start Nginx

↓

Enable Nginx

↓

Copy Configuration

↓

Restart Service
```

All these tasks can be written in one Playbook.

---

# Why Use Playbooks?

Playbooks are:

- Reusable
- Easy to maintain
- Version controlled
- Human-readable
- Repeatable
- Idempotent

---

# Playbook File Extension

```text
.yml
```

Example:

```text
install-nginx.yml

user-management.yml

deploy-app.yml
```

---

# Basic Playbook Structure

```yaml
---
- name: Install Nginx
  hosts: web
  become: true

  tasks:
    - name: Install Nginx
      dnf:
        name: nginx
        state: present

    - name: Start Nginx
      service:
        name: nginx
        state: started
```

---

# Main Components

Every Playbook usually contains:

- name
- hosts
- become
- gather_facts
- vars (optional)
- tasks
- handlers (optional)

---

# Name

Provides a readable description.

```yaml
- name: Install Apache Web Server
```

---

# Hosts

Specifies where the Playbook will run.

```yaml
hosts: web
```

Run on all servers

```yaml
hosts: all
```

---

# Become

Executes tasks with root/sudo privileges.

```yaml
become: true
```

Without Become

```yaml
become: false
```

---

# Gather Facts

Collects system information before running tasks.

```yaml
gather_facts: true
```

Disable

```yaml
gather_facts: false
```

Disabling facts speeds up Playbook execution when system information isn't required.

---

# Tasks

Tasks are the individual operations performed by the Playbook.

```yaml
tasks:
  - name: Install Git
    package:
      name: git
      state: present

  - name: Start Service
    service:
      name: nginx
      state: started
```

---

# Variables

Variables can be defined inside a Playbook.

```yaml
vars:
  package_name: nginx

  service_name: nginx
```

Use variables

```yaml
tasks:
  - name: Install Package
    package:
      name: "{{ package_name }}"
      state: present
```

---

# Gather Facts

Common facts include:

```yaml
ansible_hostname

ansible_distribution

ansible_os_family

ansible_kernel

ansible_processor

ansible_default_ipv4.address
```

Example

```yaml
- debug:
    var: ansible_hostname
```

---

# Multiple Plays

One Playbook can contain multiple Plays.

```yaml
---
- name: Configure Web Servers
  hosts: web

  tasks:
    - name: Install Nginx
      package:
        name: nginx
        state: present

- name: Configure Database Servers
  hosts: database

  tasks:
    - name: Install MySQL
      package:
        name: mysql
        state: present
```

---

# Execution Order

Tasks execute from top to bottom.

Example

```
Task 1

↓

Task 2

↓

Task 3

↓

Task 4
```

If a task fails, Playbook execution usually stops unless error handling is configured.

---

# Running a Playbook

```bash
ansible-playbook playbook.yml
```

Specify Inventory

```bash
ansible-playbook -i hosts.ini playbook.yml
```

Verbose Mode

```bash
ansible-playbook playbook.yml -v
```

Check Mode

```bash
ansible-playbook playbook.yml --check
```

Syntax Check

```bash
ansible-playbook playbook.yml --syntax-check
```

List Tasks

```bash
ansible-playbook playbook.yml --list-tasks
```

List Hosts

```bash
ansible-playbook playbook.yml --list-hosts
```

---

# Error Handling

Ignore an error

```yaml
ignore_errors: true
```

Example

```yaml
- name: Stop Apache
  service:
    name: httpd
    state: stopped
  ignore_errors: true
```

---

# Register Output

Store command output for later use.

```yaml
- name: Check Uptime
  command: uptime
  register: uptime_output
```

Display Output

```yaml
- debug:
    var: uptime_output.stdout
```

---

# Basic Handler Example

```yaml
tasks:
  - name: Copy Config
    copy:
      src: nginx.conf
      dest: /etc/nginx/nginx.conf
    notify: Restart Nginx

handlers:
  - name: Restart Nginx
    service:
      name: nginx
      state: restarted
```

Handlers run **only when notified**.

(Handlers will be covered in detail in a later chapter.)

---

# Best Practices

- Give every task a meaningful name.
- Keep Playbooks small and reusable.
- Use variables instead of hard-coded values.
- Use modules instead of shell commands.
- Use `--check` before running in production.
- Group related tasks into separate Playbooks.
- Follow proper YAML indentation.

---

# Interview Questions

### What is a Playbook?

A YAML file that automates one or more tasks on managed nodes.

---

### Which command executes a Playbook?

```bash
ansible-playbook playbook.yml
```

---

### What does `hosts` specify?

The target managed nodes where the Playbook will run.

---

### What is `become: true`?

Runs tasks with elevated (sudo/root) privileges.

---

### What does `gather_facts` do?

Collects information about managed nodes before tasks are executed.

---

### In what order are tasks executed?

Sequentially from top to bottom.

---

### How do you perform a dry run?

```bash
ansible-playbook playbook.yml --check
```

---

### How do you verify Playbook syntax?

```bash
ansible-playbook playbook.yml --syntax-check
```

---

# Summary

- Playbooks are YAML files used to automate tasks.
- A Playbook consists of Plays, Tasks, and Modules.
- `hosts` defines the target systems.
- `become` provides elevated privileges.
- `gather_facts` collects system information.
- Multiple Plays can exist in one Playbook.
- Tasks execute sequentially.
- Use `ansible-playbook` to execute Playbooks.
- Use `--check` and `--syntax-check` before production deployments.

---
